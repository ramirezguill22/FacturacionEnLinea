Comandos para ejecutar el proyecto localmente

1. Instalar Node.js LTS en Windows si aun no esta instalado:
   winget install OpenJS.NodeJS.LTS

2. Cerrar y volver a abrir la terminal de VS Code.

3. Verificar que Node.js y npm esten disponibles:
   node -v
   npm -v

4. Entrar a la carpeta raiz del proyecto:
   cd c:\Users\guill\Documents\Portal_Facturacion_Online

5. frontend local:
   npm install

6. npm run dev

   --$env:Path = 'C:\Program Files\nodejs;' + $env:Path; npm.cmd run dev

7. Probar el frontend en el navegador:
   http://localhost:3000

8. Entrar a la carpeta del backend:
   cd c:\Users\guill\Documents\Portal_Facturacion_Online\backend

9. backend:
   npm install

10. backend local:
    npm run dev

    -- $env:Path = 'C:\Program Files\nodejs;' + $env:Path; npm.cmd run dev

11. Probar el endpoint del backend:
    POST http://localhost:8080/api/tickets/validar

    Body JSON:
    {
      "numeroTicket": "TICKET-PRUEBA-0000001"
    }

12. Comando PowerShell para probar el endpoint:
    Invoke-RestMethod -Method Post -Uri 'http://localhost:8080/api/tickets/validar' -ContentType 'application/json' -Body '{"numeroTicket":"TICKET-PRUEBA-0000001"}'

Resultado esperado del frontend:
Facturacion en linea

Resultado esperado del backend con configuracion real valida:
Respuesta JSON devuelta por NetSuite y propagada por el backend.

Configuracion minima de Git:
git config --global user.name "ramirezguill22"
git config --global user.email "gramirez@grupocoresa.com"

Comandos basicos para subir cambios:
git add .
git commit -m "mensaje"
git push

====================
BLOQUE RAPIDO FRONTEND
====================

Carpeta:
cd c:\Users\guill\Documents\Portal_Facturacion_Online

Instalar dependencias:
npm install

Levantar frontend si npm funciona normal:
npm run dev

Levantar frontend si PowerShell no reconoce node:
$env:Path = 'C:\Program Files\nodejs;' + $env:Path; npm.cmd run dev

URL del frontend:
http://localhost:3000

===================
BLOQUE RAPIDO BACKEND
===================

Carpeta:
cd c:\Users\guill\Documents\Portal_Facturacion_Online\backend

Instalar dependencias:
npm install

Levantar backend si npm funciona normal:
npm run dev

Levantar backend si PowerShell no reconoce node:
$env:Path = 'C:\Program Files\nodejs;' + $env:Path; npm.cmd run dev

URL base del backend:
http://localhost:8080

Endpoint:
POST http://localhost:8080/api/tickets/validar

Nota:
Si abres la ruta en el navegador veras "Cannot GET /api/tickets/validar" porque el endpoint solo acepta POST.

=====================
BLOQUE RAPIDO PRUEBAS
=====================

Caso 1. Ticket encontrado:
Invoke-RestMethod -Method Post -Uri 'http://localhost:8080/api/tickets/validar' -ContentType 'application/json' -Body '{"numeroTicket":"TICKET-PRUEBA-0000001"}'

Resultado esperado:
Respuesta de NetSuite para el caso `encontrado`.

Caso 2. Ticket no encontrado:
Invoke-RestMethod -Method Post -Uri 'http://localhost:8080/api/tickets/validar' -ContentType 'application/json' -Body '{"numeroTicket":"TICKET-PRUEBA-9999999"}'

Resultado esperado:
Respuesta de NetSuite para el caso `no_encontrado`.

Caso 3. Error de validacion por longitud:
try { Invoke-RestMethod -Method Post -Uri 'http://localhost:8080/api/tickets/validar' -ContentType 'application/json' -Body '{"numeroTicket":"123"}' } catch { $_.ErrorDetails.Message }

Resultado esperado:
Respuesta local de validacion del backend antes de llamar a NetSuite.

==============================
BLOQUE RAPIDO ETAPA 4.1 LOCAL
==============================

Objetivo:
Dejar listo el backend para usar configuracion segura local desde un archivo .env no versionado.

Archivo local sugerido:
c:\Users\guill\Documents\Portal_Facturacion_Online\backend\.env

Base recomendada:
PORT=8080
TICKET_MIN_LENGTH=20
MOCK_FOUND_TICKET=TICKET-PRUEBA-0000001
NETSUITE_RESTLET_URL=
NETSUITE_ACCOUNT=
NETSUITE_REALM=
NETSUITE_CONSUMER_KEY=
NETSUITE_CONSUMER_SECRET=
NETSUITE_TOKEN_ID=
NETSUITE_TOKEN_SECRET=

Nota:
El archivo backend\.env no debe subirse al repositorio. Solo usar backend\.env.example como plantilla.

==============================
BLOQUE RAPIDO ETAPA 4.2 LOCAL
==============================

Objetivo:
Crear una capa tecnica dedicada para NetSuite dentro del backend sin reemplazar todavia el endpoint publico mock.

Archivos clave creados:
backend\src\integrations\netsuite\netsuite-tba.ts
backend\src\integrations\netsuite\netsuite-restlet-client.ts
backend\src\services\netsuite-ticket.service.ts

Que hace esta etapa:
- genera el header OAuth 1.0 para TBA
- prepara la llamada HTTP al RESTlet de NetSuite
- controla timeout, errores de red y respuesta JSON invalida
- deja encapsulada la llamada en una capa separada del endpoint publico

Que no hace todavia:
- no normaliza aun la respuesta final para el frontend
- no conecta todavia la UI en tiempo real con esta respuesta real

==============================
BLOQUE RAPIDO ETAPA 4.3 LOCAL
==============================

Objetivo:
Conectar el endpoint publico POST /api/tickets/validar con la capa real de NetSuite.

Estado actual:
- el endpoint ya usa la capa tecnica real de NetSuite
- el backend mantiene la validacion local de entrada
- la respuesta del endpoint depende de que backend/.env tenga valores reales y validos

Importante:
Si backend/.env tiene valores de ejemplo o credenciales no validas, el endpoint respondera con error de integracion y no con el mock anterior.

